import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Render to HTML root element through calling render() method 
// on ReactDOM

ReactDOM.render(
  <App />,
  document.getElementById('root')
)

